# Claude Phone Configuration

# Terminal Access (HTTP Basic Auth)
# These credentials protect the web terminal interface
CP_USER=admin
CP_PASSWORD=your_secure_password_here

# API Access (Bearer Token)
# This token is used for REST API authentication
CLAUDE_PHONE_API_TOKEN=your_secret_api_token_here

# Optional: Server Configuration
# PORT=8000
# HOST=0.0.0.0